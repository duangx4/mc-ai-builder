# MC AI Builder v2 — 重构升级路线图（合并版）

> 合并依据（2026-08-26）：
> - **A 官方源码**：`github.com/Justcnds/mc-ai-builder`（GPL-3.0，58⭐，React19+Vite7+Three.js+Express5+Electron）→ 已克隆为 v2 基线
> - **B 本地实战成果**：`MC\builder` 仓库 10 个提交（基线 9b25e99）
> - **C 改版者日志**：`Downloads\新建 文本文档.md`（2026-08-01~02 个人开发者对同款官方仓库的重制记录，40+ 轮迭代）
> - **D 技术路线讨论**：LLM 代码生成 + SDF 体素化 + 结构库 RAG + 网络素材采集（2026-08-26 OpenClaw 会话）

---

## 0. 一句话定位

**把"AI 写建筑代码 → 沙箱执行 → 3D 渲染/导出"做成真正可用的产品**：智商（生成质量）靠多 Agent 流水线与材质直通，眼睛（渲染）靠原版级引擎，肌肉（几何）靠 SDF 体素化，记忆（先验）靠结构库检索 —— **LLM 当大脑，SDF 当手，结构库当记忆**。

## 1. 现状盘点

| 资产 | 状态 | 价值 |
|------|------|------|
| 官方源码（v2 基线，5 commits） | ✅ 可跑（3001 后端 + 5173 前端冒烟通过） | 正统底座，6 技能完整 |
| B：builder 10 提交 | 未迁入 | 大门形制/中式风格/提示词优化器/生图预览 |
| C：改版者日志 | 未利用（信息资产） | 一份写好的实施手册：架构/渲染/流式全套方案 |
| D：SDF/RAG/素材采集 | 未开工 | 差异化增量 |

## 2. 借鉴改版者（C）的五大精华

### 2.1 架构：合并为"智能构建"引擎 + 多 Agent 流水线
- 三模式（快速/工作流/自主）合并为单一引擎：**阶段流（规划→构建→验证→精修→完成）当护栏 + 全工具自主决策**，阶段状态机替代字符串解析
- **分区构建**：编排器（风格规划 Agent → 区块分配 Agent）→ 并行构建 Agent；复杂建筑递归细分（深度默认 2，每节点 2~6 子区块）
- **只重建受影响区块**：修改场景仅唤醒相关区块，根治"每次优化重生成整座建筑"
- **区块衔接规划**：全局水平基准面 + 每区块地面高度/设计总高/关键高度线 + 衔接关系（轴/侧/对齐/过渡），校验强制贴边连续、墙顶檐口对齐
- **材质直通**：删 18 风格固定映射 + 26 份风格文档 → 1300+ 本地材质库注入提示词 + "材质一致性铁律"，删除逐块随机（确定性材质）

### 2.2 渲染：原版级引擎
- 原版贴图图集 + 合批渲染 + 顶点色 tint；**6 版本 4797 张贴图**动态切换（26.2/1.21.5/1.20.1/1.16.5/1.12.2/1.8.9）
- 形状合法化：楼梯/门/栅栏/玻璃板/墙按原版模型重写、邻居推断连接；11 种功能性放置物形状（梯子/轨道/酿造台/告示牌）
- 半透明三分类 + 树叶内部面剔除 + 水相邻面剔除
- 阴影：相机范围 500、实时阴影、自研 12 采样黄金角柔化、光源面板、柔化滑块
- 发光彩色化（火把琥珀/灵魂火青蓝/海晶灯白蓝）+ 局部掩膜 + 辉光开关；水菲涅尔/涟漪/高光；雾效自适应+开关
- 排查范本：阴影三连坑（相机范围/模糊半径 1 纹素/软阴影类型）、发光崩溃、石英分面 404

### 2.3 AI 工作流体验
- 全模式流式化：思考/工具/输出增量实时上屏（80ms 节流）、思考面板逐 token、工具参数流、状态徽章
- 渐进式渲染：流式回复按 `// N. 标题` 分段，边出边渲染
- 取消 15 轮迭代上限（护栏保留：空响应熔断/超时重试/用户中断）
- 相机清单级单次适配；修改场景专属指引（跳过规划/先定位/禁整体重生成）

### 2.4 工程化
- 类型检查 + 测试底座；统一设置 Schema（单一默认值来源 + 旧配置迁移）
- AI 服务层：四档思考强度、视觉开关、超时 + 指数退避重试、修复流式跨包丢数据（AI 代码缺字符根因）
- 进程健康检测（10s 巡检：AI 停滞/构建进度/中断，徽章分级）
- UI 黑白极简：三栏可调、折叠、按需加载

### 2.5 新功能
- 投影查看器：.litematic/.schem 逐层裁剪/聚焦/爆炸视图/材料统计（教学向）

## 3. 我们的增量（D，改版者没做的）

### 3.1 几何自由度：SDF 体素化原语
- 给 VoxelBuilder 加 `voxelizeSDF(fn, aabb, step)`：AI 传数学函数 f(x,y,z)，包围盒内格点采样填充。**纯算法无需模型**
- 效果：椭圆穹顶/旋转体塔楼/曲面屋顶等任意隐式曲面
- 配套：常用曲面体素模板资源（拱券/穹顶/哥特尖拱）兜底
- 进阶：OBJ/GLB 导入体素化（低模优先），用户可上传参考模型

### 3.2 结构库检索增强（RAG）
- 素材：jar 提取的 980 官方结构 + 网上采集的 .schem 作品
- 自主模式新增工具 `search_structure(query)`：检索相似结构的尺寸/布局/材料分布作参考
- 效果："数据先验"廉价版——LLM 获得真实建筑比例常识，且可溯源

### 3.3 网络素材采集（三条管道，全部验证可达）
| 管道 | 来源 | 许可 | 管线 |
|------|------|------|------|
| MC 结构 .schem/.litematic | Planet Minecraft、GitHub 数据集、Modrinth | 个人自用 OK，发布标注 | NBT 解析（现成工具）→ 案例 md → 技能库/RAG |
| 3D 模型 GLB/OBJ | Poly Haven / Sketchfab / Quaternius / OpenGameArt（全 CC0/CC-BY，探测 200） | CC0/CC-BY | 体素化器（3.1 进阶）→ 案例 md |
| 建筑图/投影图 | 开源图纸库、图集扫描 | 按来源 | 视觉参考模式（已有）整理成库 |

### 3.4 工程补强
- 模型配置本地持久化（现为浏览器 localStorage `mc-ai-settings`）
- 结构合理性检查前端化（旧 analyzeStructure.py 思路内置）
- 打包链补全（electron-builder 配置、CI）

## 4. 技术路线定性（备忘）

- **路线**：LLM 生成过程式代码（VoxelBuilder JS）→ 沙箱执行 → 体素数据。属"LLM 生成"范式，比直接吐 JSON 省 token、结构正确性靠代码逻辑保证、可复用可迭代、确定性可回放
- **不做**：自训体素扩散（数据/算力/调参三座山）；Mesh/SDF 神经生成（非本项目轨道）
- **做**：把 Mesh/SDF 路线的**算法优点**（几何自由度）内化为 SDF 工具层；把扩散路线的**数据先验优点**内化为结构库检索层

## 5. 合并路线图（分阶段，P0 先行）

### P0 打底（能跑、能回滚）
- [ ] 官方基线 ✅（已完成）
- [x] 迁入 B：builder 10 提交（gates/中式/优化器/生图预览），对比 9b25e99 逐条筛选 —— ✅ 2026-08-28 完成（c65c4be/ed5dee0/9b12a5a）
- [x] 工程化：npm scripts 统一、lint/test、设置 Schema 迁移 —— ✅ 2026-08-28 完成（bf5c544/48545b5/e7bd9ae/c985a00/af27fd4）
- [x] AI 服务层：四档思考强度、超时退避、流式跨包修复 —— ✅ 2026-08-28 完成（c90a51f/1d063d8/f7ebe09/0d1f88e；SSE 跨包缓冲 sseParser.js、fetchWithRetry.js 统一超时+指数退避+Retry-After、thinkingEffort off/low/medium/high）

### P0 全部完成 ✅（2026-08-28）

### P1 生成质量（改版者核心价值，照搬+适配）
- [x] 合并为"智能构建"引擎（阶段流护栏 + 自主决策）—— ✅ 2026-08-28 完成（97f44b4/ab9cfcf/df4790a：五阶段状态机 planning→construction→validation→refinement→done + 工具护栏 + BuildingPlan 解析 + App.jsx smart 模式，默认仍 fast 可回滚，85 测试全绿）
- [x] 多 Agent 分区流水线（编排器 + 并行构建 + 递归细分 + 只重建受影响区块）—— ✅ 2026-08-29 完成（561278d/2dc7ba7/80bd265/a887be1：partitionEngine.js 766 行，partitionPlan 递归细分 maxBlockSize=24/深度 2/子块 2~6、diffBlocks 只重建受影响、mergeBlockCodes 带区块标记+去重、runPartitionedBuild 并发 2、smartEngine 自动启用；设置项 smartPartition/maxBlockSize/partitionMaxDepth；113 测试全绿）
- [x] 区块衔接规划（水平面/高度线/衔接表/校验）—— ✅ 2026-08-29 完成（1f3f0cf/244301c/218db87：adjacencyEngine.js 衔接表+焊缝校验，gap 自动缝合/高度线差≤3 拉齐/重叠 fatal 重生成；附默认版本切 1.20.1——用户拍板主玩版本，129 测试全绿）
- [ ] 材质直通：1300+ 材质库 + 一致性铁律，删随机

### P2 渲染与体验
- [ ] 原版图集渲染管线（可先借用 C 的资产清单找原仓库要贴图）
- [ ] 阴影/发光/水/雾全套（按 C 的排查范本走）
- [ ] 全模式流式化 + 思考面板 + 状态徽章 + 渐进式渲染
- [ ] 投影查看器

### P3 差异化（我们的增量）
- [ ] SDF 体素化原语 + 曲面模板库
- [ ] 结构库 RAG（search_structure 工具）
- [ ] 素材采集管线（管道 1 先行：schematic 下载器 → 案例库）
- [ ] OBJ/GLB 导入体素化（低模优先）
- [ ] 模型配置持久化、结构检查前端化

## 6. 需要拍板的决策点

1. **三模式去留**：改版者激进弃快速模式；保守方案 = 保留快速模式作兜底、默认走新引擎。倾向：**默认新引擎，快速模式降级为"快速预览"**（复用旧代码，不维护双轨）
2. **风格知识去留**：改版者删光 18 风格注入；我们讨论过"知识+RAG"路线。倾向：**知识文档保留为技能库工具供 agent 按需读（read_subdoc），不再强行注入**——两全
3. **素材版权底线**：CC0 随便用；Sketchfab/PMC 需标注；发布形态（个人自用 vs 开源分发）影响采集范围
4. **渲染实现**：自己按 C 重造 vs 找改版者仓库直接拿渲染引擎（若有开源）。**资源清单先行**（6 版本贴图能拿到就省一半工）

## 7. 铁律（延续旧仓库教训）

- 所有改动在 v2 上 git 提交（已 5 commits 基线），改前 diff、改后 commit；旧 `ai bulider/` 归档不动
- 技能 official/ 与 user/ 双目录同步（或依赖运行时 syncOfficialToUser）
- 中文注释与业务文案
- GPL-3.0：保留原作者信息，衍生代码开源；素材许可兼容（CC0/CC-BY 无冲突，注意 CC-BY 署名）
- 只接"做完没一眼看得出"的活：每阶段有明确验收点（冒烟测试、渲染截图、案例库条目数）

## 附：验收点速查

| 阶段 | 验收标准 |
|------|------|
| P0 | `npm run dev` + `node server.js` 双端通；B 成果全部可见 |
| P1 | 大型建筑（100+ 格）修改局部只重建区块；材质无随机混搭 |
| P2 | 阴影/发光/水效果截图达标；流式思考上屏 |
| P3 | 生成含曲面建筑；search_structure 返回合理参考；案例库 ≥100 条 |