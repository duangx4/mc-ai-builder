# MC AI Builder v2 — 重构升级计划

> 创建：2026-08-26 晚（OpenClaw 主 agent）
> 性质：基于**官方完整源码**（github.com/Justcnds/mc-ai-builder，GPL-3.0，58⭐）的新项目，替代旧 exe 逆向工作流

## 一、为什么新建这个项目

旧工作流（`ai bulider/` → `MC/builder/`）的根问题是：**没有源码**，只能靠逆向 pkg 打包的 exe / dist bundle 改字符串再重打包，改一行都要过 `extract → patch → rebuild`，且 `ai bulider` 副本曾被反复改坏（技能库 6→4 删改事故）。

今天确认了官方 GitHub 仓库 = **exe 的完整原始源码**（React+Vite 前端、Express 后端、6 技能系统、Electron 打包配置全都在），彻底绕开逆向：

| 能力 | 旧（逆向工作流） | v2（源码工作流） |
|------|------|------|
| 改前端/后端 | 从 bundle 抠字符串 → patch | 直接改 .jsx/.js → `npm run dev` 热更新 |
| 技能系统 | 6 技能被删成 4，需修复 | 官方 6 技能（knowledge/planning/construction/inspection/decoration/quality）完整 |
| 构建发布 | pkg 手打 | `npm run build` / `electron:build` |
| 回滚 | 无 | git 全量历史 |

## 二、现状（2026-08-26 晚已落地）

- [x] 项目目录：`C:\Users\21972\OneDrive\Desktop\MC\mc-ai-builder-v2`
- [x] 官方源码基线落地（git init，4 个提交，origin 指向官方仓库）
- [x] 依赖安装：654 包（npmmirror + electron 镜像）
- [x] 冒烟测试通过：`node server.js` → 3001 正常出 `/api/skills`（6 技能齐全）；`npm run dev` → vite 5173 320ms 就绪
- [x] docs/ 4 份文档补齐（B站视频文档 / 自定义脚本API文档 / 自定义脚本使用指南 / 视频宣发文案，含 GBK 文件名修复）

## 三、待办：把本地 10 个提交的成果迁入 v2

`C:\Users\21972\OneDrive\Desktop\MC\builder` 仓库（基线 9b25e99 之上）有 10 个提交的实战改进，**逐个评估迁入**：

| 提交 | 内容 | 迁入建议 |
|------|------|------|
| 29f2255 | 大门形制知识三层覆盖（gates.md + setDoor 警示 + 中式大门） | ✅ 直接迁（construction/knowledge 技能 + prompt-optimizer） |
| 33998ec | 提示词优化器防重复声明 + 顶层 const 去重兜底 | ✅ 直接迁（prompt-optimizer.js） |
| 61c6620 | 生图预览工作流（image-preview.js 渲染正面图） | ⚠️ 看 v2 是否已有 imagegen，评估合并 |
| 6baeb13 | 优化器带上下文，防 LLM 臆造风格 | ✅ 直接迁 |
| dcd10a4 | 提示词优化功能注入输入框 | ✅ 直接迁（前端） |
| aae3fbc | 中式古典风格 chinese_classical（梁思成《中国建筑史》） | ✅ 直接迁（knowledge-skill resources） |
| 2ebfdbb / 868f83b | gitignore 修复 / playwright MCP 忽略 | ✅ 抄配置 |
| bfe0e3a / 339fc86 | 归档 ai bulider / 目录收敛 | 归档性质，不迁 |
| 9b25e99 | baseline 原始可用版本 | 与官方源码对比，仅迁差异 |

**对比方法**：`git -C ..\builder diff 9b25e99..HEAD -- src/skills/ dist/prompt-optimizer.js` 后手工筛选。

## 四、v2 升级方向（候选，待用户拍板优先级）

1. **工程化**：dev 热更新打通 ✅ → 补 CI/lint/test → 打包链（electron-builder 配置补全）
2. **AI 引擎升级**：
   - 三模式（快速/自定义/自主）保留，修复 agent 循环 toolcall 历史过滤坑（OpenAI `insufficient tool messages`，旧补丁可参考）
   - 模型适配：OpenAI 兼容多 provider（现在 API key 存在浏览器 localStorage `mc-ai-settings`，改造成本端配置持久化）
   - 生成质量控制：接入 `analyze_structure.py` 思路的前端化（旧版是工具脚本，v2 可做成内置检查器）
3. **技能体系**：官方 6 技能为基础 + 迁入本地增强（gates/中式/优化器）→ 开放技能 CRUD UI
4. **功能增强候选**：多版本方块映射（官方已有 blocks_1_21.json 体系）、导出格式补全、建筑库/模板社区
5. **发布形态**：保持 Electron 桌面 + 可选 web 模式

## 五、运行方式（v2）

```bash
cd C:\Users\21972\OneDrive\Desktop\MC\mc-ai-builder-v2
npm install          # 已完成
npm run dev          # 终端1：vite 前端 http://localhost:5173
node server.js       # 终端2：后端 http://localhost:3001
# 浏览器开 5173（vite 代理 /api → 3001）
# 打包：npm run dist（electron-builder）
```

## 六、铁律（沿用旧仓库教训）

- 所有改动在 v2 上进行并 git commit；旧 `ai bulider/` 不再动（归档）
- 技能 `official/` 与 `user/` 双目录同步，改前 `git diff`，改后 commit
- 所有业务说明/SKILL/注释保持中文
- GPL-3.0 许可：保留原作者信息，衍生代码开源